export enum Speed {
  Auto = "auto",
  Slow = "slow",
  Normal = "normal",
  Fast = "fast",
  Extreme = "extreme",
}

export enum GlobalCardModifierIndex {
  First = 0,
  AddOrSubtract = 1,
  MultiplyOrDivide = 2,
  Last = 3,
}
